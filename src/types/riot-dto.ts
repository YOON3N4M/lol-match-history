export interface RiotAccountDto {
  puuid: string;
  gameName: string;
  tagLine: string;
}

export interface SummonerDto {
  profileIconId: number;
  // long이 뭐여
  revisionDate: any;
  puuid: string;
  // long
  summonerLevel: any;
}
